// Talos CI Action — placeholder entry point.
// Build the real bundle with:  cd action && npm run build
console.error("Talos CI Action has not been built. Run: cd action && npm run build");
process.exit(1);
